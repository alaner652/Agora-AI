# 前端環境變數樣板 —— 本機開發複製成 frontend/.env.local。

# 後端公開 URL（client / 瀏覽器端，build-time 烘入）。
# - 反向代理（同源）部署：留空 → 走相對路徑 /api、/login…，image 通用、換 host 免重 build。
# - 本機開發 / 直連後端（無 proxy）：設成 http://localhost:8000。
NEXT_PUBLIC_API_URL=http://localhost:8000

# 後端內網 URL（server component / serverFetch，runtime 讀取、不烘進 bundle）。
# 本機開發可省略（會退回 NEXT_PUBLIC_API_URL 或 localhost:8000）；docker-compose 設為 http://backend:8000。
# API_INTERNAL_URL=http://backend:8000
